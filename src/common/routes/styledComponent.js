import styled from '@emotion/styled';
import tw from 'tailwind.macro';


const MainHeadingStyle = styled.div`font-size:30px;font-weight:bold;${tw `flex justify-center p-3`}`;

export {MainHeadingStyle};