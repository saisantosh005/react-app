import styled from '@emotion/styled';
import tw  from 'tailwind.macro';

const CheckOutButtonStyle=styled.button`color:gray;
            background:black;
            font-size:25px;width:50%;${tw `py-1 my-5 `}`;

export {CheckOutButtonStyle};