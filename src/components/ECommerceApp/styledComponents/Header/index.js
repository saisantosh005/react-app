import styled from '@emotion/styled';
import tw from 'tailwind.macro';


const SortDivStyle = styled.div `${tw `flex  items-center`}`;
const HeaderMainDivStyle = styled.div `${tw `flex justify-between`}`;
const Select =styled.select`${tw `ml-1`}`
export {SortDivStyle,HeaderMainDivStyle,Select}