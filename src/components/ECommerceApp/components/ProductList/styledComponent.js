import styled from '@emotion/styled';
import tw  from 'tailwind.macro';

const ProductListStyle =styled.div`width:100%;${tw `flex flex-wrap items-center mt-3`}`;
const ProductBodyDivStyle =  styled.div `width:70%;${tw `flex  flex-col `}`;
export {ProductListStyle,ProductBodyDivStyle};