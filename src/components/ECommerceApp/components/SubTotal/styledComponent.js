import styled from '@emotion/styled';
import tw  from 'tailwind.macro';

const SubTotalStyle = styled.div `width:90%;${tw `flex justify-between items-center mx-6`}`;

const AmountStyle = styled.div `color:gold`;

const SubTotalName = styled.div `color:#70758c;font-size:22px;`;

export {SubTotalStyle,AmountStyle,SubTotalName};